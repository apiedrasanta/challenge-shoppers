import React from 'react';

const Layout = (props) => (
  <div className="container">
    { props.children }
  </div>
);

// Layout.propTypes = {};

// Layout.defaultProps = {};

export default Layout;